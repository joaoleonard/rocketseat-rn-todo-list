import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
    },
    counterContainer: {
        marginLeft: 10,
        backgroundColor: "#333",
        padding: 2,
        paddingHorizontal: 10,
        borderRadius: 50,
    },
    counter: {
        color: "#D9D9D9",
        fontSize: 14,
        fontWeight: "bold",
    }
});